language: node_js
node_js:
  - "lts/*"

script:
  - npm run lint
  - npm test
  - npm run coverage
jobs:
  include:
    # Define the release stage that runs semantic-release
    - stage: Release
      node_js: lts/*
      deploy:
        provider: script
        skip_cleanup: true
        script:
          - npx semantic-release
